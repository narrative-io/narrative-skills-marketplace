{
  "$schema": "https://agentskills.io/schemas/skills-index.v0.json",
  "generated_by": "scripts/gen-skills-index.ts",
  "plugins": [
    {
      "name": "narrative-audience",
      "version": "0.1.0",
      "description": "Audience-building workflows backed by the narrative-mcp server — turning marketing briefs into filtered, materialized audience datasets and activating them to downstream advertising connectors.",
      "mcpServers": [
        "narrative-agent-gateway",
        "narrative-knowledge-base",
        "narrative-mcp"
      ]
    },
    {
      "name": "narrative-common",
      "version": "0.1.0",
      "description": "Common Narrative workflows backed by the narrative-mcp server — starting with Rosetta Stone attribute mapping generation, evaluation, and improvement.",
      "mcpServers": [
        "narrative-agent-gateway",
        "narrative-knowledge-base",
        "narrative-mcp"
      ]
    },
    {
      "name": "narrative-connector-dev",
      "version": "0.1.0",
      "description": "Build Narrative data connectors agentically — a spec-driven skill set that carries a connector from spec through service code, infrastructure, platform registration, frontend listing, and deploy/verify, stopping at every human gate. Filesystem/git work across narrative-connectors, narrative-db, and narrative-platform-ui.",
      "mcpServers": []
    },
    {
      "name": "narrative-identity",
      "version": "0.1.0",
      "description": "Identity-graph workflows backed by the narrative-mcp server — pre-graph data-quality auditing, edge-quality analysis, and graph-build hygiene.",
      "mcpServers": [
        "narrative-agent-gateway",
        "narrative-knowledge-base",
        "narrative-mcp"
      ]
    }
  ],
  "skills": [
    {
      "name": "create-lookalike",
      "version": "0.1.1",
      "description": "Create a look-alike audience from a seed audience and a candidate\npopulation dataset. Classifies Rosetta Stone attributes, generates\nthe same materialized-view scoring pipeline Lookalike Studio emits\n(Naive-Bayes categorical weights + Gaussian continuous similarity),\ngates on approval, submits via `narrative_workflows_create`, and\nmonitors the build to completion.\nUse when: \"create a lookalike audience\", \"find more users like this\nsegment\", \"expand my seed audience to 500k similar users\", \"score\nthe population against my customers\", \"build a look-alike of\ndataset X\".\n(narrative-audience)",
      "plugin": "narrative-audience",
      "path": "plugins/narrative-audience/skills/create-lookalike/SKILL.md",
      "compatibility": "Requires the narrative-mcp MCP server and local file Read. Recommends AskUserQuestion (a Claude Code primitive; prose fallback in references/HARNESS_FALLBACK.md), the narrative-knowledge-base MCP server, and a shell with python3 (3.8+) to run scripts/lookalike_state_tag.py for UI re-edit support. Portable to any agentskills.io-compliant harness via the documented fallbacks.",
      "requirements": {
        "requires": {
          "tools": [
            "Read"
          ],
          "mcp-servers": [
            "narrative-mcp"
          ],
          "mcp-tools": [
            "narrative_context_get",
            "narrative_datasets_search",
            "narrative_datasets_describe",
            "narrative_dataset_get_column_stats",
            "narrative_attributes_search",
            "narrative_nql_validate",
            "narrative_data_planes_list",
            "narrative_workflows_create",
            "narrative_workflow_runs_list",
            "narrative_jobs_search",
            "narrative_jobs_describe"
          ]
        },
        "recommends": {
          "skills": [
            "narrative-common:generate-rosetta-stone-mappings"
          ],
          "tools": [
            "AskUserQuestion",
            "Bash",
            "Write"
          ],
          "mcp-servers": [
            "narrative-knowledge-base"
          ],
          "mcp-tools": [
            "narrative_context_search_companies",
            "narrative_context_set_company",
            "search_narrative_i_o_knowledge_base"
          ]
        }
      },
      "args": [
        {
          "name": "--seed",
          "value": "<id|name>",
          "required": false,
          "description": "The seed audience dataset — the users the output should look like. Numeric ID or datasetName. If omitted, the skill asks."
        },
        {
          "name": "--population",
          "value": "<id|name>",
          "required": false,
          "description": "The candidate population dataset to score and select from. Numeric ID or datasetName. If omitted, the skill asks."
        },
        {
          "name": "--size",
          "value": "<N>",
          "required": false,
          "description": "Output mode \"size\": keep the top N highest-scoring candidates. Mutually exclusive with --min-score."
        },
        {
          "name": "--min-score",
          "value": "<0..1>",
          "required": false,
          "description": "Output mode \"score\": keep every candidate whose similarity probability is at or above this threshold (clamped to 0.001–0.999 and converted to log-odds internally). Mutually exclusive with --size."
        },
        {
          "name": "--include-seed",
          "required": false,
          "default": false,
          "description": "Union the seed members back into the output audience with a perfect score of 1.0. Off by default — the output is look-alikes only."
        },
        {
          "name": "--name",
          "value": "<audience name>",
          "required": false,
          "description": "Unique name for the output audience dataset. Slugified to form the pipeline prefix. Must not collide with an existing dataset."
        },
        {
          "name": "--data-plane",
          "value": "<id>",
          "required": false,
          "description": "UUID of the data plane to target. Skips data-plane resolution."
        },
        {
          "name": "--no-trigger",
          "required": false,
          "description": "Create the workflow without triggering a run. Default is trigger_immediately: true — the audience builds right away."
        },
        {
          "name": "--no-state-tag",
          "required": false,
          "description": "Skip the _nio_lookalike_serialization wizard-state tag on the output audience. Default is to include it (via scripts/lookalike_state_tag.py) so Lookalike Studio can reopen the audience for editing."
        },
        {
          "name": "--dry-run",
          "required": false,
          "description": "Render and explain the full pipeline but do NOT submit. Implies --show-spec."
        },
        {
          "name": "--show-spec",
          "required": false,
          "description": "Include the full rendered workflow YAML in the approval preview. Off by default; the plain-English summary is enough for most users."
        },
        {
          "name": "<free-text tail>",
          "required": false,
          "description": "The user's intent (e.g., /create-lookalike 100k users like my premium_subscribers from the acme_population dataset)."
        }
      ]
    },
    {
      "name": "apply-rosetta-stone-mappings",
      "version": "0.3.7",
      "description": "Apply a set of Rosetta Stone attribute mappings to a Narrative\ndataset by wrapping them in a one-shot workflow that calls the\n`CreateRosettaStoneMappingsIfNotExist` task. Consumes the structured\noutput of `/generate-rosetta-stone-mappings`, normalizes the\ngenerator's snake_case to the workflow task's camelCase, re-validates\nevery expression against the dataset's current schema, gates on\nuser approval, submits via `narrative_workflows_create`, polls the\ntriggered run, and reports per-mapping created / conflict / failed.\nUse when: \"apply these mappings to dataset N\", \"create the\nRosetta Stone mappings I just generated\", \"push the mappings I\nsaved earlier to <dataset>\", \"productionize this mapping list\",\n\"submit the suggested_mappings array\".\n(narrative-common)",
      "plugin": "narrative-common",
      "path": "plugins/narrative-common/skills/apply-rosetta-stone-mappings/SKILL.md",
      "compatibility": "Requires the narrative-mcp MCP server and local file Read. Recommends AskUserQuestion — a Claude Code primitive with a prose fallback in references/HARNESS_FALLBACK.md. Portable to any agentskills.io-compliant harness via the documented fallbacks.",
      "requirements": {
        "requires": {
          "tools": [
            "Read"
          ],
          "mcp-servers": [
            "narrative-mcp"
          ],
          "mcp-tools": [
            "narrative_context_get",
            "narrative_context_search_companies",
            "narrative_context_set_company",
            "narrative_datasets_describe",
            "narrative_data_planes_list",
            "narrative_nql_validate",
            "narrative_workflows_create",
            "narrative_workflow_runs_list",
            "narrative_workflows_describe"
          ]
        },
        "recommends": {
          "skills": [
            "narrative-common:generate-rosetta-stone-mappings"
          ],
          "tools": [
            "AskUserQuestion"
          ]
        }
      },
      "args": [
        {
          "name": "--dataset",
          "value": "<id|name>",
          "required": false,
          "description": "The target dataset's numeric ID or its datasetName (alphanumerics + underscores). Required before submission; if omitted, the skill asks."
        },
        {
          "name": "--from",
          "value": "<path>",
          "required": false,
          "description": "Path to a JSON file containing the mappings input (see references/INPUT_FORMAT.md). Mutually exclusive with --mappings."
        },
        {
          "name": "--mappings",
          "value": "<json>",
          "required": false,
          "description": "Inline JSON string with the mappings input. Useful when invoked programmatically from /generate-rosetta-stone-mappings."
        },
        {
          "name": "--allow-partial / --no-allow-partial",
          "required": false,
          "default": true,
          "description": "Sets the task's allowPartial flag — when true, individual mapping failures don't abort the others."
        },
        {
          "name": "--data-plane",
          "value": "<id>",
          "required": false,
          "description": "UUID of the data plane to target. Skips data-plane resolution."
        },
        {
          "name": "--dry-run",
          "required": false,
          "description": "Render the full spec, re-validation results, and the create-call parameters; do NOT submit. Implies --show-spec."
        },
        {
          "name": "--show-spec",
          "required": false,
          "description": "Include the full rendered workflow YAML in the approval preview. Off by default — most users only need the plain-English summary."
        },
        {
          "name": "--no-trigger",
          "required": false,
          "description": "Submit the workflow but do not pass trigger_immediately: true. The user must trigger it manually later (rare)."
        },
        {
          "name": "--no-revalidate",
          "required": false,
          "description": "Skip Phase 5 NQL re-validation. Intended for same-conversation hand-off from /generate-rosetta-stone-mappings. Do NOT pass when the input is from a file, a paste, or a prior conversation — the schema may have drifted."
        }
      ]
    },
    {
      "name": "create-workflow",
      "version": "0.6.5",
      "description": "Author and submit a Narrative workflow from a natural-language\nintent. Picks the closest example from `assets/examples/`, adapts\nit to the user's case, walks the YAML against the spec, resolves\nthe data plane, and submits via `narrative_workflows_create` only\nafter the user has approved the rendered spec.\nUse when: \"create a workflow that does X\", \"schedule a daily\nrefresh of dataset Y\", \"wrap this NQL as a workflow\", \"build a\npipeline that creates view A then refreshes view B\", \"submit this\nworkflow YAML\", \"productionize this query as a recurring job\".\n(narrative-common)",
      "plugin": "narrative-common",
      "path": "plugins/narrative-common/skills/create-workflow/SKILL.md",
      "compatibility": "Requires the narrative-mcp MCP server and local file Read. Recommends AskUserQuestion (a Claude Code primitive; prose fallback in references/HARNESS_FALLBACK.md) and the narrative-knowledge-base MCP server. Portable to any agentskills.io-compliant harness via the documented fallbacks.",
      "requirements": {
        "requires": {
          "tools": [
            "Read"
          ],
          "mcp-servers": [
            "narrative-mcp"
          ],
          "mcp-tools": [
            "narrative_context_get",
            "narrative_context_search_companies",
            "narrative_context_set_company",
            "narrative_data_planes_list",
            "narrative_workflows_create",
            "narrative_workflows_describe",
            "narrative_workflows_trigger",
            "narrative_workflow_runs_list"
          ]
        },
        "recommends": {
          "skills": [
            "narrative-common:find-attribute"
          ],
          "tools": [
            "AskUserQuestion"
          ],
          "mcp-servers": [
            "narrative-knowledge-base"
          ],
          "mcp-tools": [
            "search_narrative_i_o_knowledge_base",
            "query_docs_filesystem_narrative_i_o_knowledge_base",
            "narrative_datasets_search",
            "narrative_datasets_describe",
            "narrative_nql_validate"
          ]
        }
      },
      "args": [
        {
          "name": "--spec",
          "value": "<path>",
          "required": false,
          "description": "Path to a YAML file containing the workflow specification. Skip the drafting phase and use this verbatim."
        },
        {
          "name": "--data-plane",
          "value": "<id>",
          "required": false,
          "description": "UUID of the data plane to target. Skips data-plane resolution."
        },
        {
          "name": "--trigger",
          "required": false,
          "description": "Pass trigger_immediately: true on create — fires one run as soon as the workflow is registered."
        },
        {
          "name": "--schedule",
          "required": false,
          "description": "Pass schedule_immediately: true on create — activates the schedule: cron. Requires the spec to contain a schedule: block."
        },
        {
          "name": "--tags",
          "value": "<a,b,c>",
          "required": false,
          "description": "Comma-separated tags to attach to the workflow."
        },
        {
          "name": "--dry-run",
          "required": false,
          "description": "Render and display the full spec, the chosen data plane, and the create-call parameters — but do NOT call narrative_workflows_create. Implies --show-spec."
        },
        {
          "name": "--show-spec",
          "required": false,
          "description": "Include the full rendered YAML in the approval preview. Off by default — most users only need the plain-English summary."
        },
        {
          "name": "<free-text tail>",
          "required": false,
          "description": "The user's intent (e.g., /create-workflow daily refresh of active_users at midnight UTC). With no arguments and no tail, the skill asks via AskUserQuestion."
        }
      ]
    },
    {
      "name": "design-analysis",
      "version": "0.3.5",
      "description": "Translate a fuzzy analytical question into a rigorous investigation\nplan. Interrogates the ask, grounds the plan in the available data\ndictionary, applies analytical best practices, and produces a\nstructured brief of query specifications for a downstream\nquery-writing skill. Plans, does not write SQL.\nUse when: \"why did X drop\", \"is there a relationship between A and B\",\n\"who are our highest-value customers\", \"what's driving the change\nin Y\", \"investigate this trend\", \"design an analysis for\", \"scope\nthis analytical question\".\n(narrative-common)",
      "plugin": "narrative-common",
      "path": "plugins/narrative-common/skills/design-analysis/SKILL.md",
      "compatibility": "No hard requirements — degrades to a prose planning skill when MCP is absent. Recommends the narrative-mcp MCP server and AskUserQuestion (a Claude Code primitive; prose fallback in references/HARNESS_FALLBACK.md). Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-common:write-nql"
          ],
          "tools": [
            "AskUserQuestion"
          ],
          "mcp-servers": [
            "narrative-mcp"
          ],
          "mcp-tools": [
            "narrative_context_get",
            "narrative_context_search_companies",
            "narrative_context_set_company",
            "narrative_datasets_search",
            "narrative_datasets_describe",
            "narrative_dataset_get_column_stats"
          ]
        }
      },
      "args": [
        {
          "name": "--dataset",
          "value": "<id>",
          "required": false,
          "description": "Pre-bind one or more datasets (comma-separated). Skips dataset discovery."
        },
        {
          "name": "--no-schema",
          "required": false,
          "description": "Work from a user-pasted schema only. Skip every narrative-mcp call."
        },
        {
          "name": "--brief-only",
          "required": false,
          "description": "Skip interrogation prompts when the user has already framed the question precisely. Use sparingly."
        },
        {
          "name": "<free-text tail>",
          "required": false,
          "description": "The user's analytical question. With no arguments, the skill walks the user through interrogation interactively."
        }
      ]
    },
    {
      "name": "find-attribute",
      "version": "0.3.4",
      "description": "Find the canonical Rosetta Stone attribute that best matches a\nfuzzy description, semantic phrase, or required schema shape.\nSearches the catalog with pagination, describes the shortlist in\none batched call, ranks candidates by name + shape match, and\nreturns the canonical attribute ID plus close alternatives.\nUse when: \"find the X attribute\", \"what's the graph-edge attribute\nID\", \"look up the email Rosetta Stone attribute\", \"search the\nattribute catalog for Y\", \"which attribute has SOURCE_ID +\nTARGET_ID + IS_DIRECTED\".\n(narrative-common)",
      "plugin": "narrative-common",
      "path": "plugins/narrative-common/skills/find-attribute/SKILL.md",
      "compatibility": "Requires the narrative-mcp MCP server. Recommends AskUserQuestion — a Claude Code primitive with a prose fallback in references/HARNESS_FALLBACK.md. Portable to any agentskills.io-compliant harness via the documented fallbacks.",
      "requirements": {
        "requires": {
          "mcp-servers": [
            "narrative-mcp"
          ],
          "mcp-tools": [
            "narrative_attributes_search",
            "narrative_attributes_describe"
          ]
        },
        "recommends": {
          "tools": [
            "AskUserQuestion"
          ]
        }
      },
      "args": [
        {
          "name": "--phrase",
          "value": "<text>",
          "required": false,
          "description": "The fuzzy description to search for. Same as the free-text tail; if both are given, the flag wins."
        },
        {
          "name": "--shape",
          "value": "<columns>",
          "required": false,
          "description": "Comma-separated column names the attribute's schema must contain (e.g., SOURCE_ID,TARGET_ID,IS_DIRECTED). Casing is ignored; matching is by name."
        },
        {
          "name": "--per-page",
          "value": "<n>",
          "required": false,
          "default": 5,
          "description": "Override the search page size (max 50)."
        },
        {
          "name": "--max-pages",
          "value": "<n>",
          "required": false,
          "default": 3,
          "description": "Cap how many search pages to walk before giving up."
        },
        {
          "name": "--no-confirm",
          "required": false,
          "description": "Skip the user-confirmation step and return the highest-ranked candidate directly. Use when called from another skill that handles confirmation itself."
        },
        {
          "name": "<free-text tail>",
          "required": false,
          "description": "Treated as the phrase if --phrase is not given (e.g., /find-attribute graph edge). With no arguments and no tail, the skill asks via AskUserQuestion."
        }
      ]
    },
    {
      "name": "generate-rosetta-stone-mappings",
      "version": "0.5.2",
      "description": "Generate, evaluate, and improve Rosetta Stone attribute mappings for\na Narrative dataset.\nUse when: \"map this dataset to Rosetta Stone\", \"suggest normalized\nattributes for dataset N\", \"evaluate the mappings on dataset N\", \"why\nis this mapping low confidence\", \"fix this expression\", \"improve this\nNQL mapping expression\".\n(narrative-common)",
      "plugin": "narrative-common",
      "path": "plugins/narrative-common/skills/generate-rosetta-stone-mappings/SKILL.md",
      "compatibility": "Requires the narrative-mcp MCP server. Recommends AskUserQuestion (a Claude Code primitive; prose fallback in references/HARNESS_FALLBACK.md) and the narrative-knowledge-base MCP server. Portable to any agentskills.io-compliant harness via the documented fallbacks.",
      "requirements": {
        "requires": {
          "skills": [
            "narrative-common:profile-dataset",
            "narrative-common:find-attribute"
          ],
          "mcp-servers": [
            "narrative-mcp"
          ],
          "mcp-tools": [
            "narrative_context_get",
            "narrative_context_search_companies",
            "narrative_context_set_company",
            "narrative_datasets_search",
            "narrative_datasets_describe",
            "narrative_attributes_describe",
            "narrative_nql_validate",
            "narrative_nql_run",
            "narrative_jobs_describe"
          ]
        },
        "recommends": {
          "skills": [
            "narrative-common:apply-rosetta-stone-mappings"
          ],
          "tools": [
            "AskUserQuestion"
          ],
          "mcp-servers": [
            "narrative-knowledge-base"
          ],
          "mcp-tools": [
            "search_narrative_i_o_knowledge_base",
            "query_docs_filesystem_narrative_i_o_knowledge_base"
          ]
        }
      },
      "args": [
        {
          "name": "<free-text>",
          "required": false,
          "description": "Natural-language intent naming the source dataset and what to map (e.g., \"map dataset 12345 to Rosetta Stone\", \"evaluate the mappings on dataset N\"). This skill takes no flags; it resolves the dataset and gathers context interactively. With no arguments, it asks which dataset to map."
        }
      ]
    },
    {
      "name": "profile-dataset",
      "version": "0.1.2",
      "description": "Produce a coverage & quality profile of a Narrative dataset (or access\nrule): row count, per-column null/fill rate, cardinality, ranges,\ntop-values, inferred column shape, and quality flags. Reads bundled\nstats + sample first, recovers missing/stale stats by configuring and\nrecalculating them, and escalates to a cheap `/write-nql` query only\nfor a measure no stat can provide. Descriptive, not prescriptive.\nUse when: \"profile dataset N\", \"what does dataset N look like\",\n\"coverage and quality of <dataset>\", \"what id types does N emit\",\n\"null rates / cardinality for <dataset>\", \"is this dataset's stats\nfresh\".\n(narrative-common)",
      "plugin": "narrative-common",
      "path": "plugins/narrative-common/skills/profile-dataset/SKILL.md",
      "compatibility": "Requires the narrative-mcp MCP server (no MCP → cannot profile). Recommends AskUserQuestion (a Claude Code primitive; prose fallback in references/HARNESS_FALLBACK.md), the `/write-nql` sibling skill for the custom-measure escalation, and the narrative-knowledge-base MCP server. Portable to any agentskills.io-compliant harness via the documented fallbacks.",
      "requirements": {
        "requires": {
          "mcp-servers": [
            "narrative-mcp"
          ],
          "mcp-tools": [
            "narrative_context_get",
            "narrative_datasets_describe",
            "narrative_dataset_get_column_stats",
            "narrative_dataset_set_column_stats_config",
            "narrative_dataset_recalculate_statistics",
            "narrative_dataset_request_sample",
            "narrative_jobs_describe"
          ]
        },
        "recommends": {
          "skills": [
            "narrative-common:write-nql"
          ],
          "tools": [
            "AskUserQuestion"
          ],
          "mcp-servers": [
            "narrative-knowledge-base"
          ],
          "mcp-tools": [
            "narrative_context_search_companies",
            "narrative_context_set_company",
            "narrative_access_rules_describe",
            "narrative_nql_validate",
            "narrative_nql_run"
          ]
        }
      },
      "args": [
        {
          "name": "--dataset",
          "value": "<id>",
          "required": false,
          "description": "Profile a dataset by numeric id. Mutually exclusive with --access-rule. If neither is passed, the skill asks for the id and source type (it does not search)."
        },
        {
          "name": "--access-rule",
          "value": "<id>",
          "required": false,
          "description": "Profile an access rule by id. Access rules behave like datasets in NQL, but their describe returns no bundled stats or sample — see references/ACCESS_RULES.md. Mutually exclusive with --dataset."
        },
        {
          "name": "--focus",
          "value": "<col,col,...>",
          "required": false,
          "description": "Restrict the profile to these columns (e.g. \"_rosetta_stone.graph_edge.target_id_type\"). Default: all columns."
        },
        {
          "name": "--histograms",
          "required": false,
          "description": "Include value-distribution histograms. Off by default (they can blow the response cap on wide columns); opt in when the caller needs a distribution."
        },
        {
          "name": "--allow-recalc",
          "required": false,
          "description": "Pre-approve the tier-2 stats configure + recalculate recovery step, skipping its confirmation gate. For automation / skill callers."
        },
        {
          "name": "--allow-nql",
          "required": false,
          "description": "Pre-approve the tier-3 custom-NQL escalation, skipping its confirmation gate. For automation / skill callers."
        },
        {
          "name": "--json",
          "required": false,
          "description": "Emit only the structured profile object, skipping the prose render. Use when the caller is another skill or automation."
        },
        {
          "name": "<free-text tail>",
          "required": false,
          "description": "A note about what the caller needs profiled (e.g., \"id-type distribution\", \"fill rate on the email columns\"). Steers column focus and which custom measures, if any, matter."
        }
      ]
    },
    {
      "name": "write-nql",
      "version": "0.5.6",
      "description": "Write, validate, and (optionally) execute an NQL query against a\nNarrative dataset. Drafts the query from the user's question, runs\n`narrative_nql_validate` until it compiles, explains the query in\nplain English, and only runs it on explicit approval (or when\ninvoked with `--run`).\nUse when: \"write an NQL query for X\", \"query this dataset\",\n\"validate this NQL\", \"run NQL against dataset <id>\", \"how many rows\nmatch Y\", \"show me the top N records from <dataset>\".\n(narrative-common)",
      "plugin": "narrative-common",
      "path": "plugins/narrative-common/skills/write-nql/SKILL.md",
      "compatibility": "Requires the narrative-mcp MCP server. Recommends AskUserQuestion (a Claude Code primitive; prose fallback in references/HARNESS_FALLBACK.md) and the narrative-knowledge-base MCP server. Portable to any agentskills.io-compliant harness via the documented fallbacks.",
      "requirements": {
        "requires": {
          "mcp-servers": [
            "narrative-mcp"
          ],
          "mcp-tools": [
            "narrative_context_get",
            "narrative_context_search_companies",
            "narrative_context_set_company",
            "narrative_datasets_search",
            "narrative_datasets_describe",
            "narrative_nql_validate",
            "narrative_nql_run",
            "narrative_jobs_describe"
          ]
        },
        "recommends": {
          "tools": [
            "AskUserQuestion"
          ],
          "mcp-servers": [
            "narrative-knowledge-base"
          ],
          "mcp-tools": [
            "search_narrative_i_o_knowledge_base",
            "query_docs_filesystem_narrative_i_o_knowledge_base"
          ]
        }
      },
      "args": [
        {
          "name": "--run",
          "required": false,
          "description": "Skip the end-of-flow confirmation and execute the query immediately after validation succeeds."
        },
        {
          "name": "--dataset",
          "value": "<id>",
          "required": false,
          "description": "Pre-bind the target dataset. Skips the dataset-search step."
        },
        {
          "name": "--limit",
          "value": "<n>",
          "required": false,
          "description": "Override the default LIMIT (default 100 for raw selects, no limit for aggregations)."
        },
        {
          "name": "--no-explain",
          "required": false,
          "description": "Skip the plain-English explanation. Use only when the caller is another skill or automation."
        },
        {
          "name": "<free-text tail>",
          "required": false,
          "description": "Treated as the user's question (e.g., /write-nql --dataset 12345 how many distinct users last 30 days). With no arguments, the skill walks the user through the flow interactively."
        }
      ]
    },
    {
      "name": "add-connector-app-ui",
      "version": "0.1.0",
      "description": "Build the connector's app UI in narrative-platform-ui — the profile-creation\nflow (OAuth connect button or credential fields) and the quick-settings\nform — generated from the auth and quick_settings blocks of\nconnector-spec.yaml.\nUse when: \"build the connector app UI\", \"add the profile creation form\",\n\"wire up the quick settings form\", \"add the OAuth connect button for the\nconnector\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/add-connector-app-ui/SKILL.md",
      "compatibility": "Stub — implementation pending. No hard requirements: local UI code in the narrative-platform-ui working tree, PR-reviewed; no infra or destructive ops. Reads connector-spec.yaml. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:register-connector-app",
            "narrative-connector-dev:define-connector-interface"
          ]
        }
      }
    },
    {
      "name": "add-connector-listing",
      "version": "0.1.0",
      "description": "Add the connector's marketplace listing to narrative-platform-ui — display\nname, logo, description, and categories — from connector-spec.yaml, as a\nreviewable UI change.\nUse when: \"add the connector listing\", \"list the connector in the\nmarketplace UI\", \"add the connector card\", \"register the connector in the\ncatalog UI\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/add-connector-listing/SKILL.md",
      "compatibility": "Stub — implementation pending. No hard requirements: local UI edits in the narrative-platform-ui working tree, PR-reviewed; no infra or destructive ops. Reads connector-spec.yaml. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:register-connector-app"
          ]
        }
      }
    },
    {
      "name": "add-connector-oauth",
      "version": "0.1.0",
      "description": "Add a third-party OAuth 2.0 authorization-code flow with KMS-encrypted\ntoken storage to a scaffolded connector — domains, stores, api client,\noauth service, callback + authorize routes, the token-table migration,\nand the KMS/IAM/SSM plumbing — all driven by the auth.oauth block of\nconnector-spec.yaml.\nUse when: \"add OAuth to the connector\", \"wire up the <platform> OAuth\nflow\", \"the connector needs authorization-code auth\", \"add token storage\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/add-connector-oauth/SKILL.md",
      "compatibility": "Stub — implementation pending. Mostly local codegen, but reaches the DB (token-table migration) and infra (KMS/IAM/SSM) at human gates. Reads the auth.oauth block of connector-spec.yaml. Recommends AskUserQuestion for gate confirmations. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:scaffold-connector",
            "narrative-connector-dev:provision-connector-db"
          ],
          "tools": [
            "AskUserQuestion"
          ]
        }
      }
    },
    {
      "name": "add-measurement-ingestion",
      "version": "0.1.0",
      "description": "Wire the framework measurement-feed engine into a connector — scan a\npartner S3 inbox, copy new files into a Narrative dataset ingestion\nfolder, write _NIO_COMMIT, and dedup so files are never copied twice —\ndriven by the measurement block of connector-spec.yaml.\nUse when: \"add measurement ingestion to the connector\", \"ingest the\npartner measurement feed\", \"wire up the S3 inbox scan\", \"pull conversion\nfeedback files\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/add-measurement-ingestion/SKILL.md",
      "compatibility": "Stub — implementation pending. Mostly local codegen, but creates a dataset, a cross-repo DB migration, and an inbox-bucket terraform module at human gates. Reads the measurement block of connector-spec.yaml. Recommends AskUserQuestion. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:scaffold-connector",
            "narrative-connector-dev:provision-connector-db",
            "narrative-connector-dev:scaffold-connector-infra"
          ],
          "tools": [
            "AskUserQuestion"
          ]
        }
      }
    },
    {
      "name": "build-connector",
      "version": "0.1.0",
      "description": "Orchestrate a full connector build — sequence the spec, service, infra,\nregistration, frontend, and deploy/verify skills against a single\nconnector-spec.yaml, stopping at every human gate (terraform applies,\nnarrative-db migrations, app registration, prod promotion).\nUse when: \"build a connector for <platform> end to end\", \"run the whole\nconnector build\", \"orchestrate the connector from spec to deploy\", \"take\nthe connector from spec to prod\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/build-connector/SKILL.md",
      "compatibility": "Stub — implementation pending. An orchestrator: it sequences the other skills and pauses at every human gate rather than doing destructive work itself. Reads and writes connector-spec.yaml as shared state. Recommends AskUserQuestion. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:spec-connector",
            "narrative-connector-dev:preflight-connector",
            "narrative-connector-dev:scaffold-connector",
            "narrative-connector-dev:define-connector-interface",
            "narrative-connector-dev:add-connector-oauth",
            "narrative-connector-dev:implement-partner-client",
            "narrative-connector-dev:implement-delivery-executor",
            "narrative-connector-dev:add-measurement-ingestion",
            "narrative-connector-dev:test-connector",
            "narrative-connector-dev:scaffold-connector-infra",
            "narrative-connector-dev:provision-connector-db",
            "narrative-connector-dev:register-connector-app",
            "narrative-connector-dev:add-connector-listing",
            "narrative-connector-dev:add-connector-app-ui",
            "narrative-connector-dev:deploy-connector",
            "narrative-connector-dev:verify-connector"
          ],
          "tools": [
            "AskUserQuestion"
          ]
        }
      }
    },
    {
      "name": "define-connector-interface",
      "version": "0.1.0",
      "description": "Define the connector's data contract — the audience metaschema, the\nQuickSettingsType / ParserType enums and codecs, the identifier-group\nattribute metaschema with correct $ref kinds, and the collaboration\npolicy — all generated from connector-spec.yaml.\nUse when: \"define the connector interface\", \"generate the audience\nmetaschema\", \"wire up the quick settings types\", \"build the collaboration\npolicy for the connector\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/define-connector-interface/SKILL.md",
      "compatibility": "Stub — implementation pending. No hard requirements: local codegen of schema/codec files in the narrative-connectors working tree; no infra or destructive ops. Reads connector-spec.yaml. Recommends the narrative-common find-attribute skill. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:scaffold-connector",
            "narrative-common:find-attribute"
          ]
        }
      }
    },
    {
      "name": "deploy-connector",
      "version": "0.1.0",
      "description": "Deploy the connector — quick-publish the image, terraform apply to dev,\nthen promote to prod — one gated step at a time, driven by\nconnector-spec.yaml.\nUse when: \"deploy the connector\", \"quick-publish and apply to dev\",\n\"promote the connector to prod\", \"ship the connector image\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/deploy-connector/SKILL.md",
      "compatibility": "Stub — implementation pending. Runs quick-publish, terraform apply, and prod promotion — every one a hard human gate. Reads connector-spec.yaml. Recommends AskUserQuestion for per-stage confirmation. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:test-connector",
            "narrative-connector-dev:scaffold-connector-infra",
            "narrative-connector-dev:provision-connector-db",
            "narrative-connector-dev:register-connector-app"
          ],
          "tools": [
            "AskUserQuestion"
          ]
        }
      }
    },
    {
      "name": "implement-delivery-executor",
      "version": "0.1.0",
      "description": "Implement the audience-delivery executor on the Arrow Dataset path —\nfield parsers, narrative_id routing, batching, file-delivery metrics, and\nthe runtime config wiring — driven by the delivery block of\nconnector-spec.yaml.\nUse when: \"implement the delivery executor\", \"write the audience delivery\npath\", \"wire up the Arrow reader for the connector\", \"build the connector\nexecutor\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/implement-delivery-executor/SKILL.md",
      "compatibility": "Stub — implementation pending. No hard requirements: local codegen in the narrative-connectors working tree; partner delivery happens only at the deploy/verify gate. Reads the delivery + identifier_groups blocks of connector-spec.yaml. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:scaffold-connector",
            "narrative-connector-dev:define-connector-interface",
            "narrative-connector-dev:implement-partner-client"
          ]
        }
      }
    },
    {
      "name": "implement-partner-client",
      "version": "0.1.0",
      "description": "Implement the connector's partner API client — request/response models,\nauth headers, pagination, rate-limit handling, retry/backoff, and error\nmapping — from the partner_api block of connector-spec.yaml.\nUse when: \"implement the partner API client\", \"write the <platform> API\nclient\", \"wire up the delivery endpoints\", \"handle the partner rate\nlimits\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/implement-partner-client/SKILL.md",
      "compatibility": "Stub — implementation pending. No hard requirements: local codegen in the narrative-connectors working tree; no live partner calls without confirmation. Reads the partner_api block of connector-spec.yaml. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:scaffold-connector",
            "narrative-connector-dev:add-connector-oauth"
          ]
        }
      }
    },
    {
      "name": "preflight-connector",
      "version": "0.1.0",
      "description": "Validate and enrich a connector-spec.yaml before any code is generated —\nresolve every identifier group to a canonical Rosetta Stone attribute,\npin the marketplace app_id, confirm the package-slug derivation, and flag\nevery remaining TODO. Produces a go / no-go preflight report.\nUse when: \"preflight the connector spec\", \"is this connector spec ready\nto build\", \"validate connector-spec.yaml\", \"check the connector spec\nbefore scaffolding\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/preflight-connector/SKILL.md",
      "compatibility": "Stub — implementation pending. No hard requirements: a read-and-enrich skill over connector-spec.yaml; no infra or destructive ops. Recommends the narrative-common find-attribute skill for Rosetta resolution and AskUserQuestion for confirmations. Runs on any agentskills.io harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:spec-connector",
            "narrative-common:find-attribute"
          ],
          "tools": [
            "AskUserQuestion"
          ]
        }
      }
    },
    {
      "name": "provision-connector-db",
      "version": "0.1.0",
      "description": "Author the connector's narrative-db migrations (V1/V2 + undo, plus the\nOAuth and measurement tables when those are in scope) and the RDS\nterraform, in the separate narrative-db repo — writing them for review,\nstopping before any migration or apply is run.\nUse when: \"provision the connector database\", \"write the narrative-db\nmigrations for the connector\", \"set up the connector RDS\", \"add the\nconnector db terraform\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/provision-connector-db/SKILL.md",
      "compatibility": "Stub — implementation pending. Writes migrations + terraform into the separate narrative-db repo; running migrations and terraform apply are hard human gates. Reads connector-spec.yaml. Recommends AskUserQuestion. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:scaffold-connector"
          ],
          "tools": [
            "AskUserQuestion"
          ]
        }
      }
    },
    {
      "name": "register-connector-app",
      "version": "0.1.0",
      "description": "Register the connector as a Narrative marketplace app — the marketplace-db\napp row, SSM credentials, the DSM installation, and the bearer token — via\nthe bootstrap-app flow, driven by connector-spec.yaml and gated on human\nconfirmation.\nUse when: \"register the connector app\", \"bootstrap the marketplace app\",\n\"create the app row for the connector\", \"run bootstrap-app for the\nconnector\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/register-connector-app/SKILL.md",
      "compatibility": "Stub — implementation pending. Mutates marketplace-db, SSM, S3, and DSM and needs a browser-copied DSM token — a hard human gate, per stage. Reads connector-spec.yaml. Recommends AskUserQuestion. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:provision-connector-db",
            "narrative-connector-dev:scaffold-connector-infra"
          ],
          "tools": [
            "AskUserQuestion"
          ]
        }
      }
    },
    {
      "name": "scaffold-connector",
      "version": "0.1.0",
      "description": "Generate the sbt module skeleton for a new connector — the\napi/services/stores/worker/executor/poller modules, build.sbt and\nQuickPublish entries, and ops docs — from the reference connector\ntemplate, driven entirely by connector-spec.yaml.\nUse when: \"scaffold the <slug> connector\", \"generate the connector\nmodules\", \"create the sbt skeleton for the connector\", \"stand up the\nconnector project\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/scaffold-connector/SKILL.md",
      "compatibility": "Stub — implementation pending. No hard requirements: local filesystem/git codegen in the narrative-connectors working tree; no infra, DB, or registration side effects. Reads connector-spec.yaml. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:preflight-connector",
            "narrative-connector-dev:define-connector-interface"
          ]
        }
      }
    },
    {
      "name": "scaffold-connector-infra",
      "version": "0.1.0",
      "description": "Generate the connector's <slug>-infra terraform (ECS service, ECR, security\ngroups, IAM) and CI workflows from connector-spec.yaml — writing the plans\nlocally without applying them.\nUse when: \"scaffold the connector infra\", \"generate the terraform for the\nconnector\", \"add the CI workflows for the connector\", \"set up the ECS\nservice for the connector\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/scaffold-connector-infra/SKILL.md",
      "compatibility": "Stub — implementation pending. Writes terraform + CI locally in the narrative-connectors working tree; does NOT run terraform apply (that is /deploy-connector). Reads connector-spec.yaml. Recommends AskUserQuestion. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:scaffold-connector"
          ],
          "tools": [
            "AskUserQuestion"
          ]
        }
      }
    },
    {
      "name": "spec-connector",
      "version": "1.2.0",
      "description": "Research and author a connector spec for a new destination platform —\ninterview, prior-art recon, vendor-doc research, and a differentiator\nwalk across five axes (auth, destination data model, identifiers and\nmatching, sync semantics, operational constraints) — then emit the\nmachine-readable connector-spec.yaml the rest of the plugin builds\nfrom, plus a prose spec.md. Identifiers, rate limits, and data-removal\nsemantics are never guessed; unknowns become partner questions.\nUse when: \"spec out a connector for <platform>\", \"start a new\nconnector\", \"research the <platform> connector\", \"write the connector\nspec\", \"draft connector-spec.yaml\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/spec-connector/SKILL.md",
      "compatibility": "Requires Bash, Read, Write, WebFetch, and WebSearch (or equivalent capabilities — these tools may be named differently across harnesses) for vendor-doc research, plus the narrative-common find-attribute skill (narrative-mcp) for Rosetta attribute verification. Recommends AskUserQuestion (prose fallback documented in the body), the narrative-knowledge-base MCP server for prior-art recon, and the Shortcut or Notion MCP servers for publishing the finished spec — all degrade gracefully when absent.",
      "requirements": {
        "requires": {
          "tools": [
            "Bash",
            "Read",
            "Write",
            "WebFetch",
            "WebSearch"
          ],
          "skills": [
            "narrative-common:find-attribute"
          ]
        },
        "recommends": {
          "tools": [
            "AskUserQuestion"
          ],
          "skills": [
            "narrative-connector-dev:preflight-connector"
          ],
          "mcp-servers": [
            "narrative-knowledge-base",
            "shortcut",
            "notion"
          ]
        }
      },
      "args": [
        {
          "name": "<platform>",
          "required": false,
          "description": "The destination platform to spec (e.g. mailchimp, hubspot, snapchat). If omitted, the skill asks first."
        }
      ]
    },
    {
      "name": "test-connector",
      "version": "0.1.0",
      "description": "Author and run the connector's test suite — sanity, OAuth round-trip, and\ndelivery tests against the connector-testkit Postgres testcontainer — and\nconfirm the modules compile before anything is deployed.\nUse when: \"test the connector\", \"run the connector tests\", \"add sanity\ntests for the connector\", \"does the connector compile and pass\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/test-connector/SKILL.md",
      "compatibility": "Stub — implementation pending. No hard requirements: local test authoring and execution in the narrative-connectors working tree; no deploy or destructive ops. Reads connector-spec.yaml. Runs on any agentskills.io- compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:implement-delivery-executor"
          ]
        }
      }
    },
    {
      "name": "verify-connector",
      "version": "0.1.0",
      "description": "Verify a deployed connector end to end — enqueue a delivery job, read the\nfile-delivery logs, and confirm total_rows / total_identifiers against the\nexpected baseline, plus the OAuth callback round-trip and any measurement\nsample-file commit — and return a go / no-go verdict.\nUse when: \"verify the connector\", \"run an end-to-end delivery check\",\n\"confirm the connector delivered correctly\", \"check the connector logs\nafter deploy\".\n(narrative-connector-dev)",
      "plugin": "narrative-connector-dev",
      "path": "plugins/narrative-connector-dev/skills/verify-connector/SKILL.md",
      "compatibility": "Stub — implementation pending. Enqueues live jobs and reads logs in dev (and prod after promotion) — confirm before prod verification. Reads connector-spec.yaml. Recommends AskUserQuestion. Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "recommends": {
          "skills": [
            "narrative-connector-dev:deploy-connector"
          ],
          "tools": [
            "AskUserQuestion"
          ]
        }
      }
    },
    {
      "name": "generate-identity-graph",
      "version": "0.4.6",
      "description": "Interactively build a Narrative identity graph workflow from one or\nmore first-party datasets and (optionally) third-party data sources.\nConfirms each input dataset is mapped to the Rosetta Stone graph\nedge attribute (mapping it via /generate-rosetta-stone-mappings if\nnot), then composes and submits a workflow that unions every edge\nsource and labels connected components.\nUse when: \"build an identity graph\", \"generate an identity graph\",\n\"create an identity graph\", \"stitch these datasets into a graph\",\n\"make a graph workflow\", \"label connected components on these\ndatasets\", \"I want a person graph / household graph / device graph\".\n(narrative-identity)",
      "plugin": "narrative-identity",
      "path": "plugins/narrative-identity/skills/generate-identity-graph/SKILL.md",
      "compatibility": "Requires the narrative-mcp MCP server and local file Read. Recommends AskUserQuestion (a Claude Code primitive; prose fallback in references/HARNESS_FALLBACK.md) and the narrative-knowledge-base MCP server. Portable to any agentskills.io-compliant harness via the documented fallbacks.",
      "requirements": {
        "requires": {
          "skills": [
            "narrative-common:find-attribute",
            "narrative-common:generate-rosetta-stone-mappings",
            "narrative-common:write-nql",
            "narrative-common:create-workflow"
          ],
          "tools": [
            "Read"
          ],
          "mcp-servers": [
            "narrative-mcp"
          ],
          "mcp-tools": [
            "narrative_context_get",
            "narrative_context_search_companies",
            "narrative_context_set_company",
            "narrative_datasets_search",
            "narrative_datasets_describe"
          ]
        },
        "recommends": {
          "skills": [
            "narrative-identity:triage-pregraph-data"
          ],
          "tools": [
            "AskUserQuestion"
          ],
          "mcp-servers": [
            "narrative-knowledge-base"
          ],
          "mcp-tools": [
            "search_narrative_i_o_knowledge_base",
            "query_docs_filesystem_narrative_i_o_knowledge_base"
          ]
        }
      },
      "args": []
    },
    {
      "name": "generate-match-report",
      "version": "0.7.2",
      "description": "Compare your data to a partner's data in the marketplace. Given a\ndataset you already own with person/edge data, this skill walks you\nthrough picking a partner data source to match against, choosing which\nidentifier types to match on, optionally selecting which enrichment\nattributes to attach, and then submits the report — returning overlap,\nmatch counts, and demographic coverage.\nUse when: \"how does my data compare to your marketplace\", \"compare my\ndata to [partner]\", \"how much overlap do I have with [supplier]\",\n\"run a match report\", \"match my customers against 3P data\", \"see\nwhat enrichment is available for my dataset\", or any open-ended\nquestion about marketplace overlap.\n(narrative-identity)",
      "plugin": "narrative-identity",
      "path": "plugins/narrative-identity/skills/generate-match-report/SKILL.md",
      "compatibility": "Requires the narrative-mcp MCP server and local file Read. Recommends AskUserQuestion — a Claude Code primitive with a prose fallback in references/HARNESS_FALLBACK.md. Portable to any agentskills.io-compliant harness via the documented fallbacks.",
      "requirements": {
        "requires": {
          "skills": [
            "narrative-common:profile-dataset"
          ],
          "tools": [
            "Read"
          ],
          "mcp-servers": [
            "narrative-mcp"
          ],
          "mcp-tools": [
            "narrative_context_get",
            "narrative_datasets_search",
            "narrative_datasets_describe",
            "narrative_dataset_request_sample",
            "narrative_jobs_describe",
            "narrative_jobs_search",
            "narrative_nql_validate",
            "narrative_workflows_create",
            "narrative_workflow_runs_list"
          ]
        },
        "recommends": {
          "skills": [
            "narrative-common:generate-rosetta-stone-mappings"
          ],
          "tools": [
            "AskUserQuestion"
          ],
          "mcp-servers": [
            "narrative-mcp"
          ],
          "mcp-tools": [
            "narrative_context_get_companies",
            "narrative_context_search_companies",
            "narrative_context_set_company",
            "narrative_attributes_describe",
            "narrative_attributes_search",
            "narrative_nql_run",
            "narrative_workflows_trigger"
          ]
        }
      },
      "args": [
        {
          "name": "--dataset",
          "value": "<id>",
          "required": false,
          "description": "Pre-bind the customer's dataset; skip the customer prompt."
        },
        {
          "name": "--supplier-ar",
          "value": "<id>",
          "required": false,
          "description": "Pre-bind the partner access rule; skip the partner prompt."
        },
        {
          "name": "--no-enrichment",
          "required": false,
          "description": "Identity-only run; omit step 4 and the attribute-related step-5 CTEs."
        },
        {
          "name": "--match-key",
          "value": "<attr[+attr…]|identifiers>",
          "required": false,
          "description": "Add a match channel (repeatable). identifiers (default) = graph_edge identifiers (email/phone). Any other value names Rosetta attributes joined with + and fused into one compound key, e.g. soundex_first_name+libpostal_normalized_address_array. Array-typed components (attribute type = array) are exploded with UNNEST. Pass twice to match on both channels."
        },
        {
          "name": "--array-field-handling",
          "value": "<standalone-attribute|graph-edge-json>",
          "required": false,
          "description": "Custom match keys, CUSTOMER side only. How the customer sources the key's component attributes: standalone-attribute (each mapped as its own attribute, UNNEST native arrays) or graph-edge-json (TRY_PARSE_JSON the graph_edge target_id, CAST AS ARRAY<STRING>, UNNEST). Auto-detected if omitted; both yield the identical compound key. The supplier side always goes raw-then-block regardless (SC-61797)."
        },
        {
          "name": "--dry-run",
          "required": false,
          "description": "Render the YAML and show it without submitting."
        }
      ]
    },
    {
      "name": "triage-pregraph-data",
      "version": "0.6.2",
      "description": "Audit a dataset or access rule before it joins an identity-graph\nbuild (access rules behave like datasets in NQL here). Enumerates\nfailure modes (hub identifiers, high-degree nodes, suspicious\nvalues, over-connected identifiers), tests hypotheses in parallel,\nquantifies damage by rows / edges / entities, and proposes minimal\nfilters ranked by severity. When issues are found, returns a\nvalidated `CREATE MATERIALIZED VIEW` NQL the caller can run to\nproduce a graph-ready clean source; if the data passes, says so and\nrecommends it unchanged. Plans and authors the clean-view NQL; does\nnot execute it.\nUse when: \"audit this dataset before the graph build\", \"find bad\nedges in <source>\", \"check identity data quality\", \"recommend\nfilters for the graph build\", \"quantify damage from\n<identifier_type>\", \"pre-graph DQ\".\n(narrative-identity)",
      "plugin": "narrative-identity",
      "path": "plugins/narrative-identity/skills/triage-pregraph-data/SKILL.md",
      "compatibility": "No hard requirements — degrades to a prose audit/planning skill when MCP is absent. Recommends the narrative-mcp MCP server and AskUserQuestion (a Claude Code primitive; prose fallback in references/HARNESS_FALLBACK.md). Runs on any agentskills.io-compliant harness.",
      "requirements": {
        "requires": {
          "skills": [
            "narrative-common:profile-dataset",
            "narrative-common:write-nql",
            "narrative-common:design-analysis"
          ]
        },
        "recommends": {
          "skills": [
            "narrative-common:generate-rosetta-stone-mappings"
          ],
          "tools": [
            "AskUserQuestion"
          ],
          "mcp-servers": [
            "narrative-mcp"
          ],
          "mcp-tools": [
            "narrative_context_get",
            "narrative_context_search_companies",
            "narrative_context_set_company",
            "narrative_datasets_search",
            "narrative_datasets_describe",
            "narrative_access_rules_search",
            "narrative_access_rules_describe",
            "narrative_nql_validate"
          ]
        }
      },
      "args": [
        {
          "name": "--dataset",
          "value": "<id>",
          "required": false,
          "description": "Pre-bind the source as a dataset. Skips discovery. Mutually exclusive with --access-rule."
        },
        {
          "name": "--access-rule",
          "value": "<id>",
          "required": false,
          "description": "Pre-bind the source as an access rule. Skips discovery. The audit runs identically — access rules behave like datasets in NQL. Mutually exclusive with --dataset."
        },
        {
          "name": "--entity-type",
          "value": "person|household|device|business|account",
          "required": false,
          "description": "Anchors what \"plausible\" looks like for the entity being identified. Required if not derivable from context."
        },
        {
          "name": "--graph-context",
          "value": "standalone|combined",
          "required": false,
          "default": "combined",
          "description": "Whether this source will be UNIONed with other sources in the downstream graph build (combined, the common case) or used on its own (standalone). Affects filter recommendations in Phase 6. If not passed and not derivable, the skill asks once in Phase 2."
        },
        {
          "name": "--identifier-cols",
          "value": "<col,col,...>",
          "required": false,
          "description": "Pre-declare which columns hold identifiers. Skips identifier-column discovery."
        },
        {
          "name": "--no-parallel",
          "required": false,
          "description": "Execute hypothesis queries serially (debugging or harness-limited)."
        },
        {
          "name": "--no-schema",
          "required": false,
          "description": "Work from a user-pasted schema only; skip every narrative-mcp call."
        },
        {
          "name": "<free-text tail>",
          "required": false,
          "description": "Additional context about the source system. With no arguments, the skill walks the user through interactively."
        }
      ]
    }
  ]
}
